# Use your computer's LAN IP while testing on a physical device.
EXPO_PUBLIC_API_URL="https://lifeos.getkasa.in"

# Switch on after Google OAuth credentials and redirect URIs are configured.
EXPO_PUBLIC_GOOGLE_AUTH_ENABLED=false
